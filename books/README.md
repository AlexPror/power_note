# books/

Сюда кладётся купленный справочник (Скурихин и др.).

- Файлы **не коммитятся** (см. `.gitignore`).
- В приложение попадают только отобранные строки в `src/data/foods.js` с `source: 'skurikhin'`.
- PDF на сайт и в репозиторий не выкладываем.
